# {模块名称} · 里程碑实施计划

> **计划状态**：草案 / `IMPLEMENTATION_PLAN_READY` / 执行中 / 已完成  
> **业务代码状态**：`NOT_STARTED`（总控生成时固定）  
> **创建或更新时间**：{YYYY-MM-DD}  
> **需求文档**：`{项目中已确认的路径}`  
> **STEP 文档**：`{项目中已确认的 steps-verified.md 路径}`  
> **进度记录**：`{项目中已确认的路径}`  
> **临时契约目录**：`{项目中已确认的路径}`  
> **正式契约**：`{项目中已确认的路径}`

## 1. 执行边界

- 本计划只编排 STEP，不修改需求或 STEP 正文。
- 完成标准引用原 STEP 的完成标志或验收口径。
- 发现源文档冲突时暂停执行并记录阻塞。
- 总控模式只生成计划；业务代码与正式 Contract 均未实施。

## 2. 划分依据

- 用户指定的阶段或节奏：{内容；没有则写“未指定”}
- 关键依赖：{STEP 间依赖摘要}
- 风险与外部依赖：{摘要}
- 可独立验收的交付点：{摘要}
- 采用 {N} 个里程碑的理由：{理由}

## 3. 里程碑总览

| 里程碑 | 目标 | STEP | 前置闸门 | 临时契约 |
|--------|------|------|----------|----------|
| {M1} | {一句话目标} | {STEP 编号} | {无或条件} | `{路径}` |
| {M…} | {一句话目标} | {STEP 编号} | {条件} | `{路径}` |

## 4. 里程碑详情

为每个里程碑填写一个完整小节。

### {里程碑编号}：{名称}

**目标**：{可独立验收的一句话结果}

**前置条件**：

- {依赖、环境或上一里程碑闸门}

**STEP 与顺序**：

| 顺序或批次 | STEP | 原标题 | 依赖 | 执行方式 |
|------------|------|--------|------|----------|
| {1} | {STEP-XXX} | {原文标题} | {依赖} | 单独 / 批次 `{批次名}` |

**本里程碑明确不包含**：

- {其他里程碑 STEP 或需求明确排除项}

**批次依据**：

| 批次 | STEP | 无依赖证据 | 无冲突证据 | 独立验收方式 |
|------|------|------------|------------|--------------|
| {批次名或“无”} | {STEP 列表} | {依据} | {依据} | {方式} |

**验收清单**：

| # | 引用的 STEP 完成标志或阶段验收项 | 验证方式 | 通过条件 |
|---|----------------------------------|----------|----------|
| 1 | {原文引用或准确摘要} | {命令、测试或检查} | {明确结果} |

**完成产物**：

- 进度记录更新：`{路径}`
- 临时契约：`{路径}`

**进入下一里程碑的闸门**：{全部 STEP 和验收项通过；或用户已确认的其他条件}

## 5. STEP 覆盖检查

| STEP | 所属里程碑 | 批次 | 是否唯一归属 |
|------|------------|------|--------------|
| {STEP-XXX} | {M*} | {批次或单独} | 是 |

确认：STEP 无遗漏、无重复归属，且原文未被修改。

## 6. 阻塞与偏差记录

| 日期 | 里程碑 / STEP | 问题证据 | 影响 | 状态 | 用户决策 |
|------|---------------|----------|------|------|----------|
| {日期} | {编号} | {证据} | {影响} | 待确认 / 已解决 | {决策} |

## 7. 契约生命周期

1. STEP 完成：记录该 STEP 的契约增量草稿。
2. 里程碑通过：汇总本阶段临时契约，不修改正式契约。
3. 所有里程碑通过：读取全部临时契约，按项目既有结构整合正式契约。
4. 整合完成：保留临时契约快照并更新进度记录。
